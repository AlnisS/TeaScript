PRINT(15, test())
PRINT(16, test()+test())
PRINT(17, printalias(1234))

PRINT(F, fib(9))
END()

FDEF(fib())
  VARIABLE(ans, a1)
  BRANCH(a1 <= 1, fibend)
  VARIABLE(ans, fib(a1-1) + fib(a1-2))
  LABEL(fibend)
EFDEF()

FDEF(printalias())
  VARIABLE(ans, a1)
EFDEF()

FDEF(test())
  VARIABLE(ans, 2.5)
EFDEF()