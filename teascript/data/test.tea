FDEF(init())
  
EFDEF()

FDEF(main())
  USERFUN(runner())
  END()
EFDEF()

FDEF(runner())
  VARIABLE(i, 0)
  LABEL(runner.start)
  IF(i <= 9)
    USERFUN(run(i))
    VARIABLE(i, i + 1)
    GOTO(runner.start)
  ENDIF()
EFDEF()

FDEF(run())
  #PRINT(a1)
  IF(a1 == 0)
    IF(true)
      PRINT("zero")
    ENDIF()
  ELIF(a1 == 1)
    PRINT("one")
  ELIF(a1 == 2)
    PRINT("two")
  ELIF(a1 == 3)
    PRINT("three")
  ELSE()
    IF(a1 <= 5)
      PRINT("many!      ah ah ah!")
    ELIF(a1 <= 7)
      PRINT("very many! ah ah ah!")
    ELSE()
      PRINT("too many!  ah ah ah!")
    ENDIF()
  ENDIF()
EFDEF()