FDEF(init())
  
EFDEF()

FDEF(main())
  USERFUN(runner())
  END()
EFDEF()

FDEF(runner())
  VARIABLE(i, 0)
  LABEL(runner.start)
  IF(i <= 4)
    USERFUN(run(i))
    VARIABLE(i, i + 1)
    GOTO(runner.start)
  ENDIF()
EFDEF()

FDEF(run())
  IF(a1 < 2)
    IF(a1 == 0)
      PRINT("zero")
    ELSE()
      PRINT("one")
    ENDIF()
  ELSE()
    IF(a1 == 2)
      PRINT("two")
    ELSE()
      IF(a1 == 3)
        PRINT("three")
      ELSE()
        PRINT("many!    ah ah ah!")
      ENDIF()
    ENDIF()
  ENDIF()
EFDEF()