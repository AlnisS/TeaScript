#weird logic test
PRINT(6, true==true!=false)

#variable test
VARIABLE(test, 0)
LABEL(a)
  PRINT(N, test)
  VARIABLE(test, test+1)
  BRANCH(test<5, a)
PRINT(7, test)

#string printing test
PRINT(8, "test")

#weird string printing test
PRINT(9, "test +  1 < 123 PRINT(9) BRANCH(EEECG)))")
#END()

#for loop test
VARIABLE(test, 1)
LABEL(loop1)
BRANCH(test > 3, endloop1)
  PRINT(N, test)
  VARIABLE(test, test + 1)
  GOTO(loop1)
LABEL(endloop1)

PRINT(N, "whoo")
#scope test
VARIABLE(test, 1)
UPSCOPE()
VARIABLE(test, 2)
PRINT(10, test)
DOWNSCOPE()
PRINT(11, test)
END()