PRINT(15, test())
PRINT(16, test()+test())
PRINT(17, printalias(1234))
PRINT(18, addtest(1, 2))

PRINT(F, fib(9))
PRINT(A, fib(6))
PRINT(FB, fib(fib(6)+1))
END()

FDEF(addtest())
  VARIABLE(ans, a1+a2)
EFDEF()

FDEF(fib())
  VARIABLE(ans, a1)
  BRANCH(a1 <= 1, fibend)
  VARIABLE(ans, fib(a1-1) + fib(a1-2))
  LABEL(fibend)
EFDEF()

FDEF(printalias())
  VARIABLE(ans, a1)
EFDEF()

FDEF(test())
  VARIABLE(ans, 2.5)
EFDEF()